#include <stdio.h>
/**
 * main - A program that prints the size of various types
 * Return: 0 (Successful)
 */
int main(void)
{
	char a;
	int b;
	long int c;
	long long int d;
	float e;

	sizeof(a);
	sizeof(b);
	sizeof(c);
	sizeof(d);
	sizeof(e);
	return (0);
}
