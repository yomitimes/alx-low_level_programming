#include <stdio.h>
/**
 * main - A program that prints alphabets.
 * Return: 0 (Success)
 */
int main(void)
{
char i;
for (i = 'a'; i <= 'z'; i++)
if( i != 'e')
if(i != 'q')
{
putchar(i);
}
putchar('\n');
return (0);
}
